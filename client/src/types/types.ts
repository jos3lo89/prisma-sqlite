export interface loginDataI {
  correo: String;
  clave: String;
}

export interface tareasI {
  descripcion: string;
  id: string;
  titulo: string;
  userId: string;
}

export interface newTaskI {
  titulo: String;
  descripcion: string;
}

export interface registeUserI {
  nombre: string
  apellido: string
  correo: string
  clave: string
}
